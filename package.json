{
  "devDependencies": {
    "cypress": "^9.4.0"
  },
  "scripts": {
    "start": "cypress open",
    "cy:chrome:run": "npx cypress run --headless --browser chrome",
    "cy:chrome:ci": "npx cypress run --headless --browser chrome",
    "cy:chrome:ci:rec": "cypress run --headless --browser chrome --tag 'production chck'"
  }
}
